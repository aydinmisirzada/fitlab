image: maven:latest
  
stages:
  - build
  - test
  - deploy
  
build-dev:
    stage: build
    only:
        - dev
    script:
        - mvn clean install -DskipTests
  
test-dev:
    stage: test
    only:
        - dev
    script:
        - mvn test
  
deploy-dev:
  stage: deploy
  only:
        - dev
  script:
    - mvn azure-webapp:deploy
